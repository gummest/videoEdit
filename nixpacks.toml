[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm install --prefix apps/api", "npm install --prefix apps/web"]

[phases.build]
cmds = ["npm run build --prefix apps/web"]

[start]
cmd = "npm --prefix apps/api start"
